FROM apache/superset:latest

USER root

# Ensure pip exists in venv and install psycopg2
RUN /app/.venv/bin/python -m ensurepip --upgrade && \
    /app/.venv/bin/python -m pip install --no-cache-dir psycopg2-binary

USER superset